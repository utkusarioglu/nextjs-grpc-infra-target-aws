version: "3.9"
services:
  terraform:
    volumes:
      - vscode-extensions:/root/.vscode-server/extensions
      - vscode-extensions-insiders:/root/.vscode-server-insiders/extensions
      - ~/.config/gh:/terratest/.config/gh:ro
      - ~/.kube:/terratest/.kube
      - ~/.aws/credentials:/terratest/.aws/credentials:ro
      - go:/go
    command: /bin/sh -c "while sleep 1000; do :; done"

volumes:
  vscode-extensions:
    name: nextjs-grpc-infra-aws-vscode-extensions
  vscode-extensions-insiders:
    name: nextjs-grpc-infra-aws-vscode-extensions-insiders
  go:
    name: nextjs-grpc-infra-aws-go
